# Mobile client

The mobile client uses the same `@iol/typescript-sdk` contract as the web client. Native implementation can be added with React Native or Expo without moving business logic out of the Laravel API.
