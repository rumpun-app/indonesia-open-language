# @iol/language-schema

Language packages are versioned data bundles. A package can provide vocabulary, phrases, grammar, lessons, scripts, and audio references without changing the core API application.

The package manifest schema is intentionally small; each referenced dataset retains its own provenance and license metadata.
